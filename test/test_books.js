//var buildResponse = require()
var assert = require('assert');

describe('buildResponse', function () {
  describe('succesful response', function () {
    it('should have result field with result value', function () {
      assert.equal(1,1);
    })
  })
});